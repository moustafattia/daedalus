[build-system]
requires = ["setuptools>=66", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sprints-mobile"
version = "0.3.0"
description = "Mobile adapter package for Sprints."
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Sprints"}]
dependencies = [
  "sprints-core",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
